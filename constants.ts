
import { Page } from './types';

export const NAV_ITEMS: Page[] = ['Inicio', 'VTP', 'Programa', 'Recetas', 'Config'];

// FIX: Explicitly type constants as numbers to allow comparison.
export const INITIAL_WEIGHT: number = 75; // Example initial weight in kg
export const GOAL_WEIGHT: number = 65; // Example goal weight in kg
